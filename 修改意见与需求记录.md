# 简历网站 · 修改意见与需求记录

> 本文件记录本项目从 v0.1 到现在的全部**修改意见 / 需求建议**，
> 作为可随代码一起上传到 GitHub 仓库的说明文档。
> 注意：本仓库源代码**不含任何真实个人信息**（姓名 / 学校 / 邮箱 / 电话 /
> 城市等均已替换为占位文本）。每位访问者在自己浏览器里编辑的内容
> 只保存在各自浏览器的 localStorage 中，不会进入本仓库。

---

## 一、最近一次修改（2026-08-25 之后）

### 1. 每个可编辑项都必须真正可修改
- **现象**：编辑面板里部分可编辑字段看不到、改不了。
- **根因**：`ContentField` 的白名单过滤只认「顶层键」，而模块注册的是
  点路径键（如 about 的 `card.title` / `placeholder.age` / `stat.*`），
  导致 `card` / `placeholder` / `stat` 这三个顶层对象被整个过滤掉。
- **改动**：`ContentField.vue` 的键过滤升级为「精确匹配或点路径前缀匹配」，
  任意注册的子字段都能在编辑面板里展开修改。
- **验收**：编辑面板中关于我的「卡片标题 / 年龄 / 城市 / 邮箱 / 电话 /
  状态 / 统计标签」等全部可见可改。

### 2. 编辑文本大小应同时调整元素框大小
- **现象**：选中一个元素（如段落、姓名、列表项）调「字号缩放」滑块，
  页面毫无反应，与「通过字号调整元素大小」的预期不符。
- **根因**：元素级字号补丁（`--fs-scale` + fit-content 自适应框）原先只
  由 `v-element-style` 指令应用，而该指令只挂在少数模块的标题元素上；
  其余可编辑元素（段落 / 卡片 / 列表等）没有挂 → 元素级字号不生效。
- **改动**：
  - 把元素级样式应用抽成共享函数 `applyElementStyle()`。
  - `v-editable` 指令（挂在所有可编辑元素上）也统一调用它，
    使**任意**可编辑元素都能响应元素级字号缩放与渐变强调。
  - 字号 ≠ 1 时元素框 `fit-content` 随内容/字号自适应变大变小。
- **验收**：选中任意可编辑元素 → 拖动字号滑块 → 该元素文字与框同步放大/缩小。

### 3. 模块配置浮窗：关闭改为「最小化成图标，可再展开」
- **现象**：关闭「模块配置」悬浮窗后找不到重新打开的地方。
- **改动**：把浮窗的关闭按钮改为最小化——关闭后浮窗收起为一个 ⚙ 小图标
  （停留在原位置），再次点击该图标即可重新展开；位置/收起状态持久化。
- **验收**：关闭配置浮窗 → 出现 ⚙ 图标 → 点击图标 → 浮窗重新展开。

### 4. 多用户编辑模型 + 绝不上传个人信息
- **需求**：每个访问者用本网页都能编辑**属于自己的**简历；源代码里不能
  有创建者的真实个人信息；上传到 GitHub 的只有代码和本「修改意见」文档。
- **实现现状**：
  - 所有内容存储在**浏览器 localStorage**（`resume-site.content` /
    `resume-site.content.mobile`），**按浏览器隔离**——A 浏览器改的
    内容不会影响 B 浏览器；每人编辑的都是自己那一份。
  - 内容与模板带版本号（`CONTENT_VERSION` / `TEMPLATE_VERSION`），
    版本不匹配时自动回退到源代码默认值，防止旧数据长期污染。
  - `src/content/*.js` 已全部还原为**占位文本**（不含姓名/学校/邮箱/
    电话/城市等真实信息）；`REPO_URL` 改为通用占位。
- **验收**：新访客打开看到占位模板 → 控制台改成自己的内容 → 刷新后
  保留（仅本浏览器）；换一台浏览器 / 清缓存即恢复占位模板。

### 5. 联系功能真正可用（点击复制 + 表单真实发信）
- **需求**：联系方式不是摆设，访客能真正联系到作者。
- **实现**：
  - 直达通道卡片（邮箱/微信/电话/GitHub）改为可点击：
    · 邮箱 / 微信：点击**复制到剪贴板**，值短暂变为「✓ 已复制」反馈
    · 电话：手机端 `tel:` 直拨；桌面端复制号码
    · GitHub：跳转（链接值可在编辑面板改成自己的主页）
  - 留言表单：提交 → 打开系统邮件客户端（`mailto:`），预填收件人
    （作者邮箱 = contact.values[0]）、主题、正文（姓名/回邮/留言），
    零后端、零配置，留言直达作者邮箱。
- **验收**：成品态点邮箱卡片复制成功；填表提交后弹出邮件客户端草稿。

### 6. 页脚社交图标可自由配置（7 预设 + 自定义上传 + 主题贴色）
- **需求**：取消页脚固定 3 个图标，改成每个人都能自由添加自己的媒体入口
  （B站 / 小红书 / 抖音 / CSDN + 原 GitHub / X / LinkedIn 共 7 个预设），
  可自定义每个图标的跳转网址，可上传自己的图标，图标风格自动贴合当前主题。
- **实现**：
  - 数据驱动：`footer.socials` 数组，每项 `{ id, label, url, icon? }`；
    成品页只显示「已填 url」的项（url 留空 = 编辑中）。
  - 预设 7 平台：内置 SVG path（`fill="currentColor"` → 自动跟随主题色）。
  - 自定义上传：图片转 base64 存入 → 用 **CSS mask + currentColor** 把
    任意图标渲染成主题色剪影（任何图标上传后自动贴合 4 套主题风格）。
  - 编辑面板「页脚」模块新增专用编辑器：预设一键添加 / 名称与链接编辑 /
    上传或替换图标 / 上移下移排序 / 删除。
- **验收**：编辑面板添加 B站图标 → 页面即时出现并跳转自定义链接；
  切换主题 → 所有图标颜色自动跟随；上传 PNG → 变成主题色剪影。

### 7. 「正在丰富中」卡片改为可选项（可显示/隐藏/删除）
- **需求**：实习/项目/教育模块里的「正在丰富中」占位卡，用户可以主动
  添加、也可以手动删除。
- **实现**：三个模块内容层新增 `more` 字段（`showMore` 开关 + `text` 文案），
  模板 `v-if` 控制显示；编辑面板可切换开关（= 删除/恢复）与改文案。
- **验收**：关掉 `showMore` 开关 → 卡片立即消失；打开 → 恢复。

### 8. 不想要的元素可直接删除
- **模块**：模块管理页每行有 ✕ 删除按钮（从模板移除，内容保留可再加回）。
- **列表条目**：经历/项目/教育等列表项在编辑面板可增删。
- **「正在丰富中」卡片**：见第 7 条（开关即删除）。

### 9. 导出成品职位列表可滚动播放
- **需求**：导出的独立 HTML 里，首屏职位列表（roles 轮播）不滚动。
- **根因**：导出剥离全部运行脚本，轮播靠 JS 定时器驱动 → 静态快照不播。
- **实现**：HeroModule 给角色元素写 `data-export-roles`（完整 roles JSON）；
  导出时注入一段【极小的内联脚本】读取该属性，每 2.6s 淡入淡出轮换职位。
  成品仍干净（仅这一段必要脚本，无水印/无仓库链接/无编辑器）。
- **验收**：导出的 HTML 打开后职位列表自动轮换。

### 10. 字号缩放覆盖所有元素（固定字号元素也随缩放）
- **需求**：仍有部分元素不随「字号缩放」改变（标签/徽标/说明等固定字号元素）。
- **根因**：模块内大量 `font-size: var(--fs-*)` 固定值不乘 `--fs-scale`，
  且 CSS 自定义属性在定义处求值——`:root` 的 `--fs-sm: calc(0.875rem * var(--fs-scale))`
  中 scale 固定为 :root 的 1，不会跟随模块 section 的覆盖。
- **实现**：ModuleSection 在模块 section 作用域按当前 `--fs-scale` **重定义**
  全部 `--fs-*` 字号令牌（`calc(<base>rem * scale)`），使【任意】用
  `var(--fs-*)` 的元素（含固定字号）都随模块级缩放；同时清理了模块样式里
  的手动 `calc(var(--fs-*) * var(--fs-scale))` 双重乘（保留 Skills 气泡、
  Hero 大标题的特殊倍数）。
- **验收**：模块级字号 1.4 时，标题/正文/标签/说明/徽标等所有元素按 1.4 缩放。

---

## 二、历史修改意见（v0.1 → v0.3）

### v0.3（8c16d90 起）
- 编辑器升级：元素级配置（选中单个元素可单独调字号/强调/动画/文字动画）、
  四套风格主题、技能气泡图（每气泡可单独调大小）、模块复制/粘贴为副本、
  点击元素精细框选（列表内精确到字段）、创意工坊入口占位。
- 导出独立 HTML：可隐藏署名 + 导出前「求 star」弹窗；项目经历支持自定义
  跳转链接。
- 编辑器体验：拖拽自由摆放 / 字号自适应框 / 入场动画响应+重播 /
  文字动画编辑态可验证。
- 关于我（about）模块加入应届生版模块列表；证书（certificates）模块从
  应届生版移除（用户：「证书不放了」）。
- 实习经历 / 项目经历 / 校园经历 显示「正在丰富中」占位卡片。
- 联系模块邮箱链接改为跟随内容（`mailto:` 使用填写的邮箱）。

### v0.2（fb061e6）
- 生成器大迭代：DEVICE 双端（桌面/手机）独立布局与内容覆盖。
- 编辑器全面升级 + UI 视觉优化。

### v0.1（3440339 起）
- 简历网站初版：双版本（资深版 senior / 应届生版 graduate）+ 首屏动画。
- **Edge 浏览器动画屏闪修复**（多轮）：
  - 根因 1：Edge 硬件加速被关闭 → 开启 GPU 硬件加速。
  - 根因 2：GaneshGL（OpenGL）后端 + 吸顶导航 backdrop-filter 闪烁
    → Edge 下导航改为实色背景。
  - 根因 3：首屏光晕球模糊+无限位移动画 → Edge 下静态显示。
  - 根因 4：Edge `prefers-reduced-motion: reduce` 开启 → 站点强制忽略
    系统「减少动态」偏好（`isMotionReduced()` 固定返回 false），并移除
    CSS 的 reduced-motion 动画禁用规则。
- 导出功能：导出干净的独立 HTML（无脚本 / 无站点署名 / 无仓库链接）。
- 关于我 增加；证书 移除。

---

## 三、待办 / 未来方向（可继续提建议）
- 导出成品默认在 GitHub Pages 部署（`base: '/resume-site/'`，按新仓库改名）。
- 更多模板 / 更多布局变体。
- 多语言内容管理增强。
