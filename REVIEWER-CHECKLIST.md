# Reviewer 验证清单 — editor-polish-team

> 角色：审查工程师（reviewer）
> 范围：编辑器升级整体验证 + 主题/气泡/复制粘贴走查 + 截图
> 项目：`D:\AI\DS harness\resume-site`（Vue 3 + Vite + GSAP，master，HEAD=fb061e6 v0.2）

## 基线（升级前现状，已核实 2026-04-22）

- 主题层 `src/themes/index.js`：现有 4 主题 = dark-glass（深色玻璃·默认）/ light-minimal（浅色极简）/ gold-luxury（暗金奢华）/ cyber-gradient（赛博渐变）。**注意：升级目标是要 4 套「真正不同风格」的主题（极客/沉着稳重/个性跳脱/清新）**，与现有命名/风格不同，需确认是否替换或新增，验收时逐套点检差异度。
- 右侧模块树 `ModuleRail.vue`：已有 `railExpand`（▸/▾）但只切换**名称列**缩写/全名（需求点 1 要求"整个部分展开/收缩，非只名称"——当前 row 仍显示开关/操作，需核对是否符合新需求）。
- 左侧配置浮窗 `ModuleConfigBar.vue`：当前是**模块级**配置（动画/字号/强调/摆放/变体，`updateModule` 整模块生效）。需求点 2 要求改为**元素级**（字号/特效作用于当前选中元素）——这是明显的行为变更，验收重点。
- 技能模块 `SkillsModule.vue`：现有 variant a（进度条）/ b（环形图）/ c（标签云），**无气泡图**。需求点 7 是新增气泡图组件（大小不一/气泡材质/冒泡动画/可调大小/同步手机端）。
- 编辑器地基已存在：useSelection / useLayout / useEditableRegistry / useHistory（见 ARCHITECTURE.md §11）。
- 工作树干净（git status 无改动），任务尚未开始。

## 验收清单（按团队目标 1–9）

### 1. 右侧模块区整体展开/收缩 + 展开显示全名
- [ ] 收缩态：整个模块区收敛（不只是名称缩写），省空间
- [ ] 展开态：显示模块全名
- [ ] 状态持久化（刷新保留）
- [ ] 展开/收缩不影响模块开关/排序/删除等能力

### 2. 左侧模块配置浮窗 → 元素级
- [ ] 点页面元素 → 浮窗显示该元素配置（字号/特效）
- [ ] 字号/特效修改只作用于选中元素，不波及其他元素
- [ ] 未选中元素时行为合理（回退模块级或提示）
- [ ] 修改实时预览 + 可撤销（历史）

### 3. 四套真正不同风格的主题
- [ ] 主题切换器列出 4 套（极客/沉着稳重/个性跳脱/清新…精品优先可少做）
- [ ] 逐套截图，四套视觉差异明显（配色/字体/质感/装饰）
- [ ] 每套都自洽：文字可读、对比度达标、不泄漏到控制台深色面板
- [ ] 切换平滑（theme-switching 淡入淡出）、持久化
- [ ] 与元素级配置共存（元素特效在每套主题下都正常）

### 4. 点元素 → 控制台自动跳转 + 呼吸闪烁提醒
- [ ] 点页面元素 → 控制台/配置浮窗自动跳到对应位置
- [ ] 呼吸闪烁：闪 2 下、低亮度、不刺眼、随后停
- [ ] 闪烁定位正确（元素位置高亮）

### 5. 精细框选（任何可输入字精确可选中，条目级）
- [ ] 任意文字条目可精确选中（光标级，非整块）
- [ ] 条目级（列表项）也可选中编辑
- [ ] 选中后编辑/复制粘贴正常

### 6. 元素复制/粘贴 + 拖拽摆放 + 可调大小
- [ ] 复制元素 → 粘贴出新元素（可独立编辑/删除）
- [ ] 拖拽摆放（useLayout 已有地基）在此功能下可用
- [ ] 元素可调整大小（新的）且持久化
- [ ] 复制/粘贴/拖拽/改大小全部可撤销（历史）
- [ ] 手机端行为一致

### 7. 专业技能气泡图（双端、可调大小、冒泡动画）
- [ ] 气泡大小不一（与技能值相关）
- [ ] 气泡材质/质感（非纯色圆形）
- [ ] 冒泡动画流畅、可暂停/降低（动效降级）
- [ ] 气泡图可调大小（整体 or 单气泡）
- [ ] 同步手机端（is-mobile 布局适配）
- [ ] 新 variant 或新模块正确注册进 moduleRegistry / 模板编排

### 8. 创意工坊入口（仅入口）
- [ ] 入口可见（不一定是新模块，可能是按钮/浮窗）
- [ ] 点击后不报错（可提示"敬请期待"或占位）
- [ ] 功能本身后置（不要求可用）——已写入文档说明

### 9. 更新项目文档
- [ ] ARCHITECTURE.md 或新增文档覆盖新能力（元素级配置/气泡图/复制粘贴/创意工坊/主题说明）

### 横切项
- [ ] 保留全部现有能力（文字编辑/增删排序/主题切换/设备双端/撤销重做）
- [ ] 控制台深色面板不随主题变色（强制深色）
- [ ] 手机端（<768）所有新功能可用
- [ ] `npm run build` 无报错
- [ ] 无 console 报错（截图走查时看 devtools）

## 走查 & 截图计划
1. 启动 `npm run dev`（或已有 dev server），浏览器打开验证
2. 每套主题：顶栏全貌截图 + skills 模块截图
3. 气泡图：桌面 + 手机视口截图（大小/动画/材质）
4. 复制粘贴/拖拽/调大小：操作前 → 操作后截图
5. 元素级配置：选中元素浮窗截图
6. 点元素跳转 + 呼吸闪烁：抓拍 2 帧
7. 精细框选：文字选中状态截图

## 状态
- [x] 基线阅读（themes/useTheme、ModuleRail、ModuleConfigBar、SkillsModule、useSelection、useLayout、useEditableRegistry、ConsolePanel）
- [ ] 等团队任务下发后按清单逐项验证并回填
